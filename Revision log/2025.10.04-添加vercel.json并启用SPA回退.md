# 添加 vercel.json 并启用 SPA 回退

作者：CodeBuddy 和 刘宇航  
日期：2025-10-04

## 任务目标
在项目根添加 vercel.json，配置 rewrites 将非 /api 路径重写到 /index.html，指定 buildCommand 与 outputDirectory，确保在 Vercel 上深链接可正常渲染。

## 变更内容
- 新增 vercel.json：
  - rewrites: /((?!api).*) → /index.html  #  
  - buildCommand: "npm run build"  #  
  - outputDirectory: "dist"  #  

## 验收指引（部署到 Vercel 后执行）
- 直接访问 /knowledge/:category/:slug 等深链接不 404，由前端路由渲染正确页面  #  
- 构建产物目录为 dist，部署成功且无报错  #  

## 后续任务
- 在 Vercel 设置环境变量 VITE_API_BASE_URL（区分 Preview/Production）  #  
- 内容与搜索索引按需/分片加载优化，降低主包体积  #  
- 可选：在 vercel.json 增加 headers 以启用静态资源长缓存  #