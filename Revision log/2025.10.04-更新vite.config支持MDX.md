# 更新 vite.config.js 支持 MDX 原始加载

作者：CodeBuddy 和 刘宇航  
日期：2025-10-04

## 任务目标
将 assetsInclude 扩展为 ['**/*.md','**/*.mdx']，与 import.meta.glob('../content/**/*.{md,mdx}', { as:'raw' }) 保持一致。

## 变更内容
- 修改 vite.config.js：assetsInclude 由 ['**/*.md'] → ['**/*.md','**/*.mdx']  #  
- 构建验证：执行 npm run build，确认无编译错误  #  

## 验收结果
- 构建成功（若失败请反馈错误信息以进一步修正）  #  
- 后续建议：在预览/生产环境验证 md 与 mdx 的动态加载与渲染  #  

## 后续任务
- 添加 vercel.json 配置 SPA 回退与输出目录  #  
- 设置 Vercel 环境变量 VITE_API_BASE_URL（区分 Preview/Production）  #  
- 内容与搜索索引按需/分片加载优化  #